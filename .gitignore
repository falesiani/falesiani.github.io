# Hugo build output and cache
public/
resources/
.hugo_build.lock
hugo_stats.json

# Node (Tailwind CSS for theme build)
node_modules/

# Optional: uncomment to ignore Go module checksum (Netlify uses its own)
# go.sum

# OS and editor
.DS_Store
*.swp
*.swo
Thumbs.db
